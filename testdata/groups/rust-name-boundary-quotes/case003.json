{
    "expr": "$'//****'",
    "dataset": null,
    "bindings": {},
    "code": "S0106",
    "comment": "Because the quote belongs to the variable name, the `/*` that follows it opens a real block comment — one that is never closed. jsonata 2.2.2 rejects this text with S0106 (Comment has no closing tag). The formatter's comment scan has to see it the same way (jsntrs-5xh)."
}
