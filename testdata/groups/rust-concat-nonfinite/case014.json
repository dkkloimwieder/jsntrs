{
    "expr": "$string([1/0])",
    "dataset": null,
    "bindings": {},
    "code": "D1001",
    "token": "string",
    "inherited": "jsonata.org, $string: \"Numeric infinity and NaN throw an error because they cannot be represented as a JSON number\" — one of the four documented casting rules, and one of the few places the documentation makes a builtin throw on a value rather than a type. A composite that merely contains a non-finite number takes the JSON.stringify path, where the throw comes from the replacer rather than from the bullet above — the same reading of the same sentence that jsntrs-jnv settled. docs/behaviors.md 1.3 has the full table, including the deliberate deviation for a composite containing NaN, which this group does not exercise. The documentation names no code — it publishes no error-code page at all (docs/behaviors.md 2.0) — so the code spelling is jsntrs' own: D3001 for a bare non-finite number, D1001 for a composite that contains one. That split is inherited vocabulary recorded in docs/behaviors.md 1.3, not a documented rule; the throw is what this case pins."
}
