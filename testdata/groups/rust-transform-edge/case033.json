{
    "expr": "($t := |$|{\"z\":1}|; $t(nope, 5))",
    "data": {},
    "bindings": {},
    "code": "T0410",
    "comment": "An undefined document is the one value the signature admits (it maps to the wildcard), so this fails on the count, not the type."
}
